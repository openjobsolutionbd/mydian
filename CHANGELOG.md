# Mydian — কী কী পরিবর্তন হয়েছে

এই ফাইলে সহজ ভাষায় লেখা থাকে GitHub-এ প্রতিবার push করার পর ঠিক কী কী
বদলেছে এবং কেন। কোনো টেকনিক্যাল জ্ঞান লাগবে না পড়তে — নতুন এন্ট্রি সবসময়
উপরে যোগ হয় (সর্বশেষ কাজ সবার আগে)।

---

## ২০২৬-০৮-২১ — বড় সাইজের নোট "too large" এরর দেখাচ্ছিল, এখন খুলবে

আপনার `prompts.md` নোট খুলতে গিয়ে "This file is too large to open"
এরর দেখাচ্ছিল। কারণ ছিল: যেই সিস্টেম দিয়ে নোট আনা হয়, সেটার একটা
স্বাভাবিক সীমা আছে — ১ মেগাবাইটের বেশি বড় ফাইল সরাসরি সেভাবে আনা যায় না
(এটা GitHub-এর নিজস্ব নিয়ম, কোনো bug না)। এতদিন এই ক্ষেত্রে শুধু এরর
দেখানো হতো, বিকল্প কোনো উপায় ছিল না।

এখন একটা বিকল্প পথ যোগ করা হয়েছে — ফাইল ১ মেগাবাইটের বেশি হলে অ্যাপ
স্বয়ংক্রিয়ভাবে অন্য একটা পদ্ধতিতে সেটা আনবে। এখন **১০০ মেগাবাইট
পর্যন্ত** যেকোনো নোট বা ছবি/PDF খোলা যাবে (আগের সীমা ছিল মাত্র ১ MB)।
এর চেয়েও বড় ফাইলের ক্ষেত্রে এখনো স্পষ্ট এরর মেসেজ দেখাবে (এটা GitHub-এর
একদম শেষ সীমা, এর বেশি কোনোভাবেই সম্ভব না)।

---

## ২০২৬-০৮-২০ — "সিঙ্ক হচ্ছে না" ইন্ডিকেটর এখন আরও সঠিক তথ্য দেবে

সাইডবারের উপরে যে ছোট বিন্দুটা (dot) দেখায় কতগুলো পরিবর্তন এখনো সিঙ্ক
হয়নি — আগে সবসময় "অফলাইন" রঙে (হলুদ) দেখাত, এমনকি আপনি আসলে অনলাইন
থাকলেও। এখন সত্যিই নেট না থাকলে হলুদ, আর নেট থাকা সত্ত্বেও কোনো
নির্দিষ্ট ফাইল সিঙ্ক হতে সমস্যা হলে লাল রঙে দেখাবে — যাতে বোঝা যায় এটা
নেট-সমস্যা না, একটা নির্দিষ্ট ফাইলের সমস্যা।

---

## ২০২৬-০৮-২০ — আপনার পাঠানো স্ক্রিনশট থেকে বের করা: নতুন আপডেট কখনো কখনো ফোনে দেরিতে পৌঁছাতে পারত

আপনি একটা নোট খুলে দেখিয়েছিলেন লেখা মাঝপথে কেটে যাচ্ছে, নিচে অনেকটা
জায়গা ফাঁকা। খুঁজে দেখা গেল — এই বাগটা আসলে আগেই ঠিক করা হয়েছিল
(কয়েকদিন আগে), কিন্তু সমস্যা ছিল অন্য জায়গায়: যেই সার্ভারে অ্যাপটা
থাকে (Cloudflare), সেটা মাঝেমধ্যে নতুন আপডেট হওয়ার পরেও কিছুদিন পুরনো
ভার্সন দেখাতে থাকতে পারে, নতুন করে খোলা সত্ত্বেও।

এখন এটা ঠিক করা হয়েছে যাতে ভবিষ্যতে যেকোনো ফিক্স/আপডেট আপনার কাছে
সাথে সাথেই পৌঁছায়, দেরি ছাড়াই।

**আপনার এখনই একটা কাজ করা দরকার:** অ্যাপটা সম্পূর্ণ বন্ধ করে (recent
apps থেকে সোয়াইপ করে বন্ধ করুন, শুধু ব্যাক বাটনে না) আবার খুলুন —
এতে সবচেয়ে নতুন ভার্সনটা লোড হবে, আর আগের কাটা-পড়া সমস্যাটাও ঠিক
দেখাবে।

---

## ২০২৬-০৮-১৮ — সিঙ্ক করার সময় দুইবার একসাথে চেষ্টা হওয়ার একটা বিরল সম্ভাবনা বন্ধ

অফলাইনে করা এডিট নেট ফিরলে সিঙ্ক করার প্রক্রিয়াটা একাধিক জায়গা থেকে
(অ্যাপ খোলার সময়, নেট ফেরার সময়, ফাইল খোলার সময়) শুরু হতে পারে। খুব
বিরল একটা টাইমিং-এ এই প্রক্রিয়াটা দুইবার একসাথে চলে যেতে পারত, যেটা
সম্ভাব্যভাবে একই ফাইলের জন্য দুটো সমান্তরাল সেভ পাঠিয়ে একে অপরকে ব্যর্থ
করে দিতে পারত। এখন এটা সবসময় একবারে একটাই চলবে।

---

## ২০২৬-০৮-১৮ — খুব বড় ফাইল খুলতে গেলে এখন বোধগম্য বার্তা দেখাবে

GitHub-এর একটা সীমা আছে — প্রায় ১MB-এর বড় ফাইল একভাবে সরাসরি খোলা যায়
না। আগে এই সীমার চেয়ে বড় কোনো ছবি, PDF, বা নোট খোলার চেষ্টা করলে একটা
জটিল টেকনিক্যাল ইংরেজি এরর মেসেজ দেখাত, বোঝা কঠিন ছিল আসলে কী হয়েছে।
এখন স্পষ্ট করে বলবে "This file is too large to open"।

---

## ২০২৬-০৮-১৭ — সাময়িক সার্ভার সমস্যা যেন অ্যাপকে স্থায়ীভাবে ভেঙে না দেয়

অ্যাপের অফলাইন-সাপোর্ট সিস্টেমে একটা সূক্ষ্ম ঝুঁকি ঠিক করা হয়েছে। খুবই
বিরল কোনো মুহূর্তে (যেমন ঠিক যে সময়ে নতুন আপডেট deploy হচ্ছে) যদি
অ্যাপের মূল ফাইল আনতে গিয়ে একটা সাময়িক সার্ভার এরর আসত, আগে সেই এরর
পেজটাই ভুলবশত "সংরক্ষিত" হয়ে যেত — এবং পরে কখনো অফলাইনে অ্যাপ খুললে
আসল অ্যাপের বদলে সেই এরর পেজটাই দেখাত, যতক্ষণ না পরের কোনো সফল লোড
সেটা ঠিক করে দিত। এখন এমন সাময়িক সমস্যা আর সংরক্ষিত হবে না, আগের
ভালো ভার্সনটাই সবসময় নিরাপদ থাকবে।

---

## ২০২৬-০৮-১৬ — ফাইল Move/Rename করার পর মাঝেমধ্যে ভুল "conflict" এরর আসতে পারত

অফলাইনে কোনো নোট এডিট করার পর (এখনো সিঙ্ক হয়নি) সেই নোটটা move বা
rename করলে — সিঙ্ক-বাকি এডিটটা নতুন জায়গায়/নামে ঠিকই চলে যেত, কিন্তু
একটা পুরনো হিসাব-রাখার এন্ট্রি ভুলে থেকে যেত যেটা পরে অ্যাপকে বিভ্রান্ত
করে একটা মিথ্যা "changed elsewhere" এরর দেখাতে পারত — যদিও move/rename
আসলে পুরোপুরি সফলই হয়েছিল। এখন ঠিক করা হয়েছে।

---

## ২০২৬-০৮-১৬ — একটা ফাইলের সিঙ্ক-সমস্যা যেন বাকি ফাইলগুলোকেও আটকে না দেয়

অফলাইনে করা একাধিক এডিট পরে নেট ফিরলে একে একে GitHub-এ পাঠানো হয়।
আগে যদি এর মধ্যে একটা ফাইলে কোনো স্থায়ী সমস্যা হতো (যেমন সেই ফাইলটা
ইতিমধ্যে অন্য কোথাও থেকে ডিলিট হয়ে গেছে), তাহলে সেই একটা ফাইলের
সমস্যার কারণে বাকি সব ফাইলের সিঙ্কও আটকে যেত — যদিও তাদের কোনো
সমস্যাই ছিল না। এখন একটা ফাইলে সমস্যা হলেও বাকিগুলো ঠিকভাবে সিঙ্ক
হয়ে যাবে।

---

## ২০২৬-০৮-১৫ — ব্যাকগ্রাউন্ড prefetch-এও অফলাইন এডিট সুরক্ষিত করা হলো

গতকাল যোগ হওয়া "সব নোট আগে থেকে লোড করে রাখা" ফিচারে একটা সূক্ষ্ম ফাঁক
ছিল — যদি কোনো নোট অফলাইনে এডিট করে সেভ করা হয় কিন্তু এখনো GitHub-এ
সিঙ্ক না হয়ে থাকে, এই ব্যাকগ্রাউন্ড prefetch তাও (বিরল কিছু পরিস্থিতিতে)
পুরনো/ভিন্ন ভার্সন দিয়ে আপনার এডিট নীরবে ওভাররাইট করে দিতে পারত। এখন
এমন ফাইল prefetch সম্পূর্ণ এড়িয়ে যাবে, যতক্ষণ না সেটা সিঙ্ক হয়ে যায়।

---

## ২০২৬-০৮-১৫ — একবার কোনো টেকনিক্যাল সমস্যা হলে সেটা যেন স্থায়ী না হয়ে যায়

অ্যাপের অফলাইন-সেভ ব্যবস্থার একটা গভীর সমস্যা ঠিক করা হয়েছে। আগে যদি
কোনো কারণে (খুবই বিরল, যেমন একই সময়ে একাধিক ট্যাবে অ্যাপ খোলা থাকা)
লোকাল স্টোরেজ একবার সাময়িকভাবে সমস্যা করত, তাহলে সেই সমস্যাটা পুরো
সেশনের বাকি সময়ের জন্য স্থায়ী হয়ে যেত — এমনকি আসল কারণ সরে যাওয়ার
পরও। এর মানে দাঁড়াত, অফলাইনে করা এডিট তখন থেকে আর সেভ-করে-রাখা যেত
না। এখন এমন হলে অ্যাপ নিজে থেকেই আবার চেষ্টা করবে, স্থায়ীভাবে আটকে
থাকবে না।

---

## ২০২৬-০৮-১৫ — একসাথে দুটো পপআপ খোলার সমস্যাটা পুরোপুরি বন্ধ করা হলো

আগে কয়েকবার এই একই ধরনের সমস্যা (দুটো পপআপ একসাথে খুলে স্ক্রিন এলোমেলো
দেখানো) আলাদা আলাদাভাবে ঠিক করা হয়েছিল — কিন্তু প্রতিবার নতুন কোনো
পপআপ যোগ হলে সেই ফিক্সটা আবার মিস হয়ে যাচ্ছিল, ঠিক তেমনটাই Error Log
পেজের বেলায় হয়েছিল। এবার সমস্যাটা গোড়া থেকে ঠিক করা হয়েছে — এখন
নতুন কোনো পপআপ ভবিষ্যতে যোগ হলেও এই সমস্যা আর ফিরে আসবে না।

---

## ২০২৬-০৮-১৫ — Error Log পেজে আরেকটা ছোট বাগ ঠিক করা হলো

Settings-এ গিয়ে "View Error Log" চাপলে Settings-এর জানালাটা বন্ধ না
হয়েই Error Log-এর জানালা তার উপরে খুলে যেত — দুটো জানালা একসাথে খোলা
থেকে যেত, দেখতে একটু গোলমেলে লাগত। এখন ঠিক করা হয়েছে — একটা খুললে
আগেরটা বন্ধ হয়ে যাবে। Error Log-এর জানালা এখন Escape বাটন দিয়েও বন্ধ
করা যাবে (আগে শুধু বাইরে ক্লিক করে বা "Close" বাটনেই বন্ধ হতো)।

---

## ২০২৬-০৮-১৫ — অ্যাপ খুললেই এখন সব নোট আগে থেকে প্রস্তুত থাকবে

আপনি লক্ষ্য করেছিলেন — কোনো নোটে প্রথমবার ক্লিক করলে দেরি হয়, কিন্তু
অন্য নোটে গিয়ে আবার সেটাতে ফিরলে তাড়াতাড়ি খোলে। এখন থেকে অ্যাপ খোলার
সাথে সাথেই সব নোটের কনটেন্ট নিঃশব্দে ব্যাকগ্রাউন্ডে প্রস্তুত হয়ে যায়
— তাই প্রথমবার ক্লিক করলেও এখন সাধারণত তাৎক্ষণিকভাবেই খুলবে। যেসব
নোট আগে থেকেই প্রস্তুত ছিল সেগুলো আবার আনার দরকার হয় না, তাই এটা
দ্বিতীয়বার অ্যাপ খোলার সময় প্রায় কোনো বাড়তি সময়/ডেটা লাগে না।

---

## ২০২৬-০৮-১৫ — PIN/লগইনে নেট-সমস্যার মেসেজ ভুলভাবে বাংলায় দেখাচ্ছিল

লগইন করার সময় বা ফাইল ডিলিটের আগে PIN দেওয়ার সময় যদি নেট সংযোগ না
থাকত, তাহলে error মেসেজটা বাংলায় দেখাত — বাকি পুরো অ্যাপ ইংরেজিতে
থাকলেও। এখন সেটাও ইংরেজি ("No network connection — check your
connection and try again"), যাতে ভাষা সবজায়গায় সামঞ্জস্যপূর্ণ থাকে।

---

## ২০২৬-০৮-১৫ — প্রথমবার নোট খোলার সময় এখন "Loading…" দেখাবে

আপনি বলেছিলেন নোট খুলতে সময় লাগাটা বিরক্তিকর লাগছে। খুঁজে দেখা গেল:
একই নোট দ্বিতীয়বার খুললে ঠিকই তাৎক্ষণিক খোলে (cache থেকে), কিন্তু
প্রথমবার (এখনো cache-এ নেই এমন নোট) খুললে GitHub থেকে আনতে যে সময়টুকু
লাগে, সেই সময় স্ক্রিন পুরো খালি দেখাত — মনে হতো অ্যাপ আটকে গেছে।
এখন সেই সময় একটা ছোট "Loading…" লেখা দেখাবে, যাতে বোঝা যায় অ্যাপ কাজ
করছে। অপেক্ষার সময়টা (নেটওয়ার্কের উপর নির্ভর করে) একই থাকবে, শুধু
এখন সেটা স্পষ্টভাবে বোঝা যাবে।

---

## ২০২৬-০৮-১৪ — Worker-এর নিরাপত্তা আরেকটু শক্ত করা হলো (আপনার অনুমতি নিয়ে)

আপনাকে জিজ্ঞেস করে জেনে নেওয়ার পর করা হলো — সরাসরি বিপজ্জনক কিছু ছিল
না, কিন্তু ভালো অভ্যাস হিসেবে: Worker-টা আগে যেকোনো ওয়েবসাইট থেকে আসা
request accept করত, এখন শুধু আপনার অ্যাপের নিজের ঠিকানা
(mydian-tei.pages.dev) থেকে আসা request-ই accept করবে। **এটা কার্যকর
হতে wrangler deploy করা লাগবে** — নিচে কমান্ড দেওয়া হচ্ছে।

---

## ২০২৬-০৮-১৪ — কিছু জায়গায় ভুলবশত বাংলা লেখা থেকে গিয়েছিল, ইংলিশ করা হলো

আপনার নিয়ম অনুযায়ী অ্যাপের সব লেখা ইংলিশে থাকার কথা। কিছু নতুন ফিচারে
(Quick Switcher, নতুন Error Log, Delete করার সময়ের PIN বক্স) কিছু লেখা
ভুলবশত বাংলাতেই থেকে গিয়েছিল — সেগুলো এখন ইংলিশ করা হয়েছে।

এছাড়া Quick Switcher-এ (Ctrl+K) অনেকগুলো নোট থাকলে, তীর-বাটন (↑↓) দিয়ে
নিচের দিকের ফাইলগুলোতে গেলে সেগুলো লিস্টের বাইরে চলে যেত, দেখা যেত না।
এখন সেটাও ঠিক করা হয়েছে — সবসময় সিলেক্ট করা ফাইলটা দেখা যাবে।

---

## ২০২৬-০৮-১৪ — নোটের নিচের অংশ কাটা পড়ে যাওয়া বাগ ঠিক করা হলো

কিছু নোট খুললে লেখার একটা অংশ (বিশেষত লম্বা নোটে) দেখা যাচ্ছিল না —
মাঝপথে কাটা পড়ে যাচ্ছিল, নিচে অনেকটা খালি জায়গা থেকে যেত, স্ক্রল
করেও বাকিটা দেখা যেত না। এটা মোবাইল আর ডেস্কটপ দুই জায়গাতেই হচ্ছিল।
এখন ঠিক করা হয়েছে — লম্বা নোটও এখন পুরোপুরি স্ক্রল করে দেখা যাবে।

---

## ২০২৬-০৮-১৪ — মোবাইলে অ্যাপ খুললেই এখন সরাসরি ফাইল-লিস্ট দেখা যাবে

আগে মোবাইলে অ্যাপ খুললে প্রথমে খালি স্ক্রিন দেখাত, ফাইল দেখতে
হ্যামবার্গার (☰) আইকনে আলাদা করে ট্যাপ করতে হতো। এখন থেকে অ্যাপ খুললেই
সরাসরি ফাইল-লিস্ট (sidebar) খোলা অবস্থায় দেখাবে — কোনো ফাইলে ট্যাপ
করলে আগের মতোই এটা বন্ধ হয়ে এডিটর দেখাবে।

---

## ২০২৬-০৮-১৪ — অ্যাপে কোনো error হলে এখন স্বয়ংক্রিয়ভাবে লগ হয়

আগে অ্যাপে কোনো টেকনিক্যাল সমস্যা হলে সেটা বোঝার একমাত্র উপায় ছিল
স্ক্রিনশট বা সরাসরি বর্ণনা দেওয়া। এখন থেকে এমন কিছু ঘটলে (যেমন কোনো
বাটন কাজ না করা, কোনো ফিচার হঠাৎ ভেঙে যাওয়া) সেটা নিজে থেকেই একটা
লগে জমা হয়ে যায় — Settings-এ গিয়ে **"View Error Log"** চাপলেই কী
হয়েছিল, কখন হয়েছিল দেখা যাবে। এই ডেটা শুধু আপনার ব্রাউজারেই থাকে,
কোথাও পাঠানো হয় না। সর্বোচ্চ ২০০টা এন্ট্রি রাখা হয়, তারপর পুরনোগুলো
নিজে থেকেই মুছে যায়।

---

## ২০২৬-০৮-১৩ — আরও ৩টা ছোট বাগ ঠিক করা হলো

আপনার অনুরোধে একের-পর-এক আরও কিছু বাগ খুঁজে ঠিক করা হয়েছে:

1. **Ctrl+K-এ মাঝে মাঝে দুটো পপআপ একসাথে খুলে যেত** — কোনো একটা মোডাল
   (যেমন নতুন ফাইলের নাম দেওয়ার বক্স) খোলা অবস্থায় Ctrl+K চাপলে তার
   উপর আরেকটা সার্চ বক্স খুলে যেত, স্ক্রিন এলোমেলো দেখাত। এখন হবে না।
2. **ফাইল সরানোর সময় (move) বিরল ক্ষেত্রে ডুপ্লিকেট হয়ে যেতে পারত** —
   নেট সমস্যার কারণে নতুন জায়গায় কপি হওয়ার পর পুরনোটা মুছতে ব্যর্থ
   হলে ফাইল দুই জায়গাতেই থেকে যেত, কোনো সতর্কতা ছাড়াই। এখন এমন হলে
   স্পষ্ট বার্তা দেখাবে কোন ফাইল ম্যানুয়ালি মুছতে হবে।
3. **পেছনের একটা অটোমেশন স্ক্রিপ্টের নিজের বাগ** — আপনার চোখে পড়ার
   মতো কিছু না, প্রজেক্টের নোট-ফাইল ছোট রাখার যে স্বয়ংক্রিয় ব্যবস্থা
   গতকাল যোগ হয়েছিল, তাতে একটা লেবেলিং বাগ ছিল যেটা ঠিক করা হয়েছে।

---

## ২০২৬-০৮-১৩ — খরচ কমানোর জন্য আরও কিছু নিয়ম যোগ

আগের এন্ট্রির (নিচে) মতোই — এবার প্রজেক্টের ভেতরের কাজের নিয়মেও কিছু
যোগ হলো যাতে ছোট কাজে অপ্রয়োজনীয় বাড়তি ধাপ/লেখা এড়ানো যায়। এটাও
আপনার জন্য সরাসরি চোখে পড়ার মতো কিছু না, ভেতরের ব্যবস্থাপনা মাত্র।

---

## ২০২৬-০৮-১৩ — প্রজেক্টের নোট ফাইল এখন নিজে থেকেই ছোট থাকবে

এটা সরাসরি অ্যাপের কোনো ফিচার না, তবে গুরুত্বপূর্ণ একটা ব্যবস্থাপনা
উন্নতি: প্রজেক্টের ভেতরের নোট ফাইল (`PROJECT_NOTES.md`) সময়ের সাথে
বড় হতে থাকছিল, যেটার মানে প্রতিবার কাজ করতে বসলে বেশি পড়তে হচ্ছিল —
এবং তাই খরচও বাড়ছিল। এখন থেকে এই ফাইলটা স্বয়ংক্রিয়ভাবে ছোট রাখা হবে
(সাম্প্রতিক কয়েকটা কাজের বিবরণ রেখে, পুরনোগুলো আর্কাইভে সরিয়ে) —
প্রতিবার নতুন কিছু push করার আগে এমনিতেই ঘটবে, আলাদা করে কিছু বলতে
হবে না।

---

## ২০২৬-০৮-১৩ — Quick switcher (Ctrl+K দিয়ে দ্রুত ফাইল খোঁজা)

Obsidian-এর quick switcher-এর মতো একটা ফিচার যোগ হলো। যেকোনো সময়
**Ctrl+K** (Mac-এ Cmd+K) চাপলেই একটা সার্চ বক্স খুলবে — টাইপ করলেই
মিলে যাওয়া ফাইলগুলো তাৎক্ষণিকভাবে দেখাবে (পুরো নাম হুবহু মেলাতে হয়
না, অক্ষরগুলো সঠিক ক্রমে থাকলেই যথেষ্ট)। ↑↓ দিয়ে বেছে নিয়ে Enter
চাপলেই সেই ফাইল খুলে যাবে। Sidebar-এর টপবারেও একটা 🔍 বাটন যোগ করা
হয়েছে, যাতে কীবোর্ড শর্টকাট মনে না থাকলেও ব্যবহার করা যায়।

---

## ২০২৬-০৮-১৩ — এডিটরের কার্সর আরও স্পষ্ট করা হলো

ফাইল এডিট করার সময় ব্লিঙ্কিং কার্সর (যেখানে লেখা বসছে সেই জায়গার
নির্দেশক) ঠিকমতো চোখে পড়ছিল না — খুব চিকন ছিল। এখন কার্সরের width
বাড়িয়ে দেওয়া হয়েছে, যাতে এডিট করার সময় কোথায় লেখা বসবে সেটা সহজেই বোঝা
যায়।

---

## ২০২৬-০৮-১২ — বাকি ৪টা বাগও ঠিক করা হলো (আগের কোড রিভিউ থেকে পাওয়া মোট ৬টার সবগুলোই এখন ঠিক)

1. **অফলাইনে করা এডিট আর "হারিয়ে যাওয়ার" মতো দেখাবে না** — নেট চলে গিয়ে
   কিছু লিখে সেভ করার পর, সিঙ্ক হওয়ার আগেই ফাইলটা আবার খুললে মাঝে মাঝে
   পুরনো লেখা দেখাত। এখন সবসময় আপনার আসল (সর্বশেষ) লেখাটাই দেখাবে।
2. **নাম বদলানো বা অন্য ফোল্ডারে সরানোর সময় অফলাইন এডিট হারানো বন্ধ** —
   কোনো ফাইল এডিট করে সেভ হওয়ার আগেই (অফলাইনে) rename বা move করলে,
   আগে এডিটটা হারিয়ে যেতে পারত। এখন হারাবে না।
3. **নতুন ফাইল তৈরিতে এখন সুন্দর বার্তা** — একই নামের ফাইল আগে থেকে
   থাকলে এখন স্পষ্ট বার্তা দেখাবে ("already exists"), জটিল টেকনিক্যাল
   এরর না। Drag-and-drop দিয়ে অনেকগুলো ফাইল একসাথে আনলে, নাম-সংঘর্ষ
   হওয়া ফাইলগুলোর একটা তালিকা শেষে একবারে দেখাবে (একের-পর-এক পপআপ না)।
   ছবি/স্ক্রিনশট যোগ করার সময় একই নাম হলে এখন স্বয়ংক্রিয়ভাবে আলাদা নাম
   (যেমন `screenshot-1.png`) দেওয়া হবে, আটকাবে না।
4. **ডিলিট করার সময় নেট-সমস্যা হলে এখন সঠিক বার্তা দেখাবে** — আগে নেট
   না থাকলেও সবসময় "ভুল PIN" বলত। এখন আসল কারণ (নেট সমস্যা / ভুল PIN /
   বারবার চেষ্টা) স্পষ্টভাবে বলবে। মূল লগইন স্ক্রিনও এতে একটু ভালো
   হয়ে গেছে (আগে নেট না থাকলে একটা টেকনিক্যাল ইংরেজি বার্তা দেখাত)।

এই ৪টার কোনোটাই নিরাপত্তা-সংক্রান্ত না (আগের ২টা নিরাপত্তা বাগ আলাদাভাবে
ঠিক হয়ে গেছে) — এগুলো সবই ডেটা সঠিক থাকা আর বার্তা বোধগম্য হওয়া নিয়ে।

---

## ২০২৬-০৮-১২ — মাউস দিয়ে টেক্সট সিলেক্ট করলে এখন স্পষ্ট দেখা যায়

আগে মাউস দিয়ে ড্র্যাগ করে লেখা সিলেক্ট করলে ব্যাকগ্রাউন্ড রঙ এত হালকা
(ফিকে) ছিল যে কতটুকু সিলেক্ট হয়েছে বোঝা কঠিন ছিল — ডার্ক আর লাইট, দুই
মোডেই। এখন সিলেক্ট করা অংশের ব্যাকগ্রাউন্ড আগের চেয়ে অনেক বেশি স্পষ্ট,
দুই মোডেই লেখা ভালোভাবে চোখে পড়বে। এডিটরের ভেতরে আর বাইরে (sidebar,
মোডাল ইত্যাদি) সব জায়গাতেই একইভাবে কাজ করবে।

---

## ২০২৬-০৮-১২ — লাইট মোড টগল এখন হোমপেজেই, Settings-এ ঢুকতে হবে না

আগে থিম (dark/light) বদলাতে Settings মোডাল খুলে ভেতরে গিয়ে বাটন চাপতে
হতো। এখন sidebar-এর উপরের দিকে (New file/New folder/Refresh বাটনের
পাশে) সরাসরি একটা 🌙/☀️ আইকন-বাটন যোগ করা হয়েছে — এক ক্লিকেই থিম বদলে
যাবে, Settings খুলতে হবে না। পুরনো Settings-এর টগলটাও কাজ করবে, দুটোই
সবসময় সিঙ্কে থাকবে।

---

## ২০২৬-০৮-১২ — ২টা নিরাপত্তা সমস্যা ঠিক করা হলো (কোড রিভিউ করে পাওয়া)

আপনার অনুরোধে পুরো অ্যাপ খুঁটিয়ে দেখে ৬টা সমস্যা পাওয়া গিয়েছিল, তার মধ্যে
সবচেয়ে গুরুত্বপূর্ণ ২টা (নিরাপত্তা-সংক্রান্ত) এখন ঠিক করা হয়েছে:

1. **PIN বারবার ভুল চেষ্টা করা এখন আটকানো হয়** — আগে কেউ চাইলে অসংখ্যবার
   ভিন্ন ভিন্ন PIN দিয়ে চেষ্টা করতে পারত, কোনো বাধা ছাড়াই। এখন বারবার
   ভুল করলে সাময়িকভাবে আটকে যাবে।
2. **সার্ভার এখন আরও কড়াভাবে চেক করে** — আগে যেকোনো ধরনের অনুরোধ পাস
   হয়ে যেত, শুধু ফাইল পড়া/লেখার বাইরেও। এখন শুধু অ্যাপ যা আসলে করে
   (ফাইল দেখা/সেভ/ডিলিট) সেটাই পাস হবে, অন্য কিছু না।

⚠️ **আপনার জন্য একটা কাজ বাকি:** এই ২টা ফিক্স GitHub-এ push হয়ে গেছে,
কিন্তু Worker-এ deploy না করা পর্যন্ত লাইভ হবে না। টার্মিনালে
`worker/` ফোল্ডারে গিয়ে `wrangler deploy` চালাতে হবে (আগেও এই নিয়ম
ছিল, worker-এর কোড বদলালে সবসময় এই একটা কমান্ড লাগে)।

আরও একটা ঐচ্ছিক ধাপ আছে (PIN-লকআউটকে সম্পূর্ণ শক্তিশালী করতে) —
`SETUP.md`-তে বিস্তারিত লেখা আছে, না করলেও অ্যাপ কাজ করবে।

**বাকি ৪টা সমস্যা** (ডেটা/ছোটখাটো bug, জরুরি না) এখনো ঠিক করা হয়নি —
পরে বললেই ঠিক করে দেব।

---

## ২০২৬-০৮-১০ — আগের "চেক-আপ" সিস্টেমকে আরও শক্তিশালী করা হলো

আপনি বলেছিলেন এই সুরক্ষা ব্যবস্থাটা যতটা সম্ভব শক্তিশালী করতে — তাই আগের
চেক-আপে (গতকালই বানানো হয়েছিল) আরও কয়েকটা নতুন স্তর যোগ করা হলো:

- **আরও গভীর কোড পরীক্ষা** — এখন এমন ভুলও ধরা যাবে যেমন কোনো ভেরিয়েবল
  বানান ভুল করে লেখা, যা আগে ধরা পড়ত না।
- **একটা বড় ফাঁক খুঁজে বের করে বন্ধ করা হয়েছে** — কোনো ফাইলের নাম ভুল
  বানানে লেখা থাকলে (যেমন একটা ফাইল আরেকটা ফাইলকে খুঁজে না পাওয়া) আগে
  সেটা একদমই ধরা পড়ত না, এখন ধরবে। এটাই সেই ধরনের ভুল যা "সাইলেন্টলি"
  অ্যাপ ভেঙে দেয়, কোনো error না দেখিয়ে।
- **কনফিগারেশন ফাইল যাচাই** — কোনো সেটিং ফাইলে ছোট ভুল (যেমন একটা কমা)
  থাকলে সেটাও এখন ধরা পড়বে।

প্রতিটা নতুন চেক নিজে ইচ্ছাকৃতভাবে ভুল কোড বসিয়ে টেস্ট করে দেখা হয়েছে যে
সত্যিই ধরে — শুধু "থাকলে ভালো দেখায়" এমন কিছু যোগ করা হয়নি।

**একটা জিনিস করা যায়নি:** GitHub-এর নিজস্ব সার্ভারে দ্বিতীয় একটা স্বাধীন
চেক যোগ করার চেষ্টা করেছিলাম (যাতে আমি ভুলে চেক করতে না পারলেও GitHub
নিজেই ধরে ফেলে), কিন্তু GitHub সেটা push হতে দেয়নি — আপনার বর্তমান
token-এ এই নির্দিষ্ট কাজের জন্য একটা বাড়তি অনুমতি নেই। চাইলে token-এ
"Workflows" অনুমতি যোগ করে দিলে এটা পরে যোগ করে দিতে পারব — না চাইলেও
সমস্যা নেই, বাকি ৯টা চেক পুরোপুরি কাজ করছে, এটা শুধু একটা বাড়তি স্তর
ছিল।

**যা করা যায়নি (এবং কেন):** সত্যিকারের ব্রাউজার দিয়ে ক্লিক করে-করে টেস্ট
করার একটা ব্যবস্থা (যেটা login করে, ফাইল খুলে, সেভ করে দেখত) — এই কাজের
পরিবেশে ব্রাউজার ইনস্টল করা সম্ভব হয়নি, আর অর্ধেক-কাজ-করা এমন একটা
টেস্ট যোগ করা ভুল ভরসা দিতে পারত, তাই সেটা এড়ানো হয়েছে।

---

## ২০২৬-০৮-১০ — নতুন ফিচার/পরিবর্তনে যেন বাগ ঢুকে না যায়, তার জন্য একটা "চেক-আপ" সিস্টেম বানানো হলো

আপনি বলেছিলেন এমন একটা ব্যবস্থা করতে যাতে কোনো পরিবর্তন করলে শুরুতেই বাগ
ধরা পড়ে, পরে খুঁজে বের করে ঠিক করতে না হয়। এখন থেকে **প্রতিবার push করার
আগে** একটা স্বয়ংক্রিয় চেক চলবে (এটা আমি নিজে চালাব, আপনাকে কিছু করতে
হবে না) যেটা যাচাই করে:

- কোডে কোনো টাইপো/ভাঙা সিনট্যাক্স আছে কিনা
- কোনো বাটন/অংশ ভুলে অন্য কোথাও রেফারেন্স করা আছে কিন্তু আসল জায়গায়
  নেই কিনা (এই ধরনের ভুলেই আগে অ্যাপ চুপচাপ ভেঙে যেত, কোনো error না
  দেখিয়ে)
- আগে যে ২-৩টা বড় বাগ হয়েছিল, সেগুলো আবার ফিরে এসেছে কিনা

কোনো সমস্যা পেলে push-ই হবে না, আগে ঠিক করে তারপর push হবে — অর্থাৎ
ভাঙা কোড কখনো লাইভ সাইটে পৌঁছাবে না।

---

## ২০২৬-০৮-১০ — Light mode (সাদা থিম) যোগ করা হলো, টগল বাটন দিয়ে বদলানো যায়

আপনি Light mode চেয়েছিলেন, Dark mode-ও রেখে দিতে বলেছিলেন — তাই দুটোই এখন
আছে, একটা বাটনে বদলানো যায়:

- **Settings** বাটনে গিয়ে এখন "🌙 Dark mode" / "☀️ Light mode" নামে একটা
  বাটন দেখবেন — ট্যাপ করলেই থিম বদলে যাবে।
- আপনার পছন্দ মনে রাখা হয় — একবার Light mode বেছে নিলে, পরেরবার অ্যাপ
  খুললে সেটাই থাকবে (ব্রাউজার রিফ্রেশ বা বন্ধ করলেও)।
- Light mode-এ background সাদা/হালকা বেইজ, লেখা গাঢ় — dark mode-এর মতোই
  সোনালি (amber) রঙের ছোঁয়া রাখা হয়েছে থিমটা চেনা লাগার জন্য।
- ডিফল্ট এখনো Dark mode — কেউ যদি Light mode না বেছে নেয়, আগের মতোই
  dark দেখবে।

---

## ২০২৬-০৮-১০ — Claude যাতে কম টোকেন খরচ করে কাজ করতে পারে, তার জন্য ডকুমেন্টেশন গুছানো

আপনি বলেছিলেন Claude যেন কাজ করার সময় কম টোকেন খরচ করে। মূল কারণ ছিল
`PROJECT_NOTES.md` ফাইলটা (Claude প্রতিবার কাজ শুরুতে এটা পড়ে) অনেক বড় হয়ে
গিয়েছিল — ৫৮ KB। তাই:

- পুরনো সব বিস্তারিত bug-ইতিহাস এবং শুরুর আলোচনা নতুন একটা
  `HISTORY.md` ফাইলে সরানো হয়েছে (**কিছুই মোছা হয়নি**, শুধু আলাদা করা
  হয়েছে — দরকার হলে সেখান থেকে পড়া যাবে)।
- `PROJECT_NOTES.md`-এ একই তথ্য দুইবার লেখা ছিল কিছু জায়গায় — সেগুলো
  একবার করে দেওয়া হয়েছে।
- ফলাফল: `PROJECT_NOTES.md` এখন ৫৮ KB থেকে ~১৮ KB — প্রায় ৭০% কম।
- এডিটরের ভারী ফাইলটা (`js/editor.js`) যেন Claude কখনো ভুলে পুরোটা
  খুলে না ফেলে, তার জন্য একটা স্পষ্ট সতর্কতা যোগ করা হয়েছে।

কোনো ফিচার বদলায়নি, অ্যাপ ব্যবহারে কোনো পার্থক্য বোঝা যাবে না — এটা
সম্পূর্ণভাবে পেছনের কাজ।

---

## ২০২৬-০৮-০৯ (রাত, দ্বিতীয় বার) — এই "কী পরিবর্তন হলো" ফাইলটা চালু করা হলো

আপনি বলেছিলেন প্রতিবার push করার পর কী কাজ হলো তার একটা ডকুমেন্ট রিপোতে
রাখতে। এখন থেকে **এই ফাইলটাই সেই ডকুমেন্ট** — প্রতি push-এর পর এখানে
নতুন একটা এন্ট্রি (উপরে) যোগ হবে, সহজ ভাষায়।

---

## ২০২৬-০৮-০৯ (রাত) — পুরো অ্যাপের লেখা বাংলা থেকে ইংলিশ করা হলো

আপনি বলেছিলেন সব ইংলিশ লাগবে। তাই যা যা করা হয়েছে:

- অ্যাপের ভেতরের **সব লেখা** (বাটন, মেসেজ, এলার্ট, লগইন স্ক্রিন,
  সেটিংস) এখন ইংলিশ।
- **আইকনে** আগে "মি" লেখা ছিল, এখন "M" লেখা।
- অ্যাপের নাম/বিবরণ (যেটা ইনস্টল করার সময় দেখায়) সেটাও ইংলিশ করা হয়েছে।
- কোডের ভেতরের **টেকনিক্যাল নোট/কমেন্ট** (যেগুলো আপনি অ্যাপে কখনো দেখেন
  না, শুধু ডেভেলপমেন্টের সময় কাজে লাগে) ইচ্ছাকৃতভাবে বাংলাতেই রাখা
  হয়েছে — এতে কোনো সমস্যা নেই, কারণ এগুলো অ্যাপের স্ক্রিনে কখনো দেখা যায়
  না।

⚠️ **একটা অংশ এখনো ম্যানুয়ালি লাইভ করাতে হবে:** লগইন-সম্পর্কিত ৩টা মেসেজ
("ভুল PIN" ইত্যাদি) Worker নামের একটা আলাদা অংশে থাকে, যেটা GitHub push
করলে নিজে থেকে লাইভ হয় না — আলাদাভাবে deploy করতে হয়। এই ছোট অংশটা
এখনো পুরনো (বাংলা) মেসেজ দেখাচ্ছে, যতক্ষণ না সেই deploy করা হয়।

---

## ২০২৬-০৮-০৯ (সন্ধ্যা) — ২টা বাগ ঠিক করা হলো (আপনার অনুরোধে খুঁজে বের করা)

1. **আইকন ভাঙা ছিল** — হোমস্ক্রিন/ব্রাউজার ট্যাবের আইকনে "মি"-এর বদলে
   একটা ফাঁকা/ভাঙা বাক্স দেখাত, শুরু থেকেই। এখন ঠিক করা হয়েছে, সঠিক
   অক্ষর দেখায়।
2. **পুরোপুরি অফলাইনে অ্যাপ খুলত না** — নেট একদম না থাকলে (এরোপ্লেন
   মোড, বা কয়েকদিন অ্যাপ না খোলার পর) অ্যাপটা সাদা স্ক্রিন দেখাত, একদমই
   খুলত না। এখন ঠিক করা হয়েছে — অফলাইনেও অ্যাপ খুলবে এবং আগে দেখা
   নোটগুলো পড়া যাবে।

---

## তার আগের কাজ (সংক্ষেপে)

- সব ফাইলের সাইজ ছোট করে অ্যাপ দ্রুত করা হয়েছে (এডিটরের সাইজ প্রায়
  অর্ধেক)।
- অ্যাপ থেকে বাংলা ফন্ট (Hind Siliguri) সরিয়ে ফেলা হয়েছে, শুধু ইংলিশ
  ফন্ট (Inter) ব্যবহার হয় — এটাই এখনকার নিয়ম, নতুন করে বাংলা ফন্ট আর
  যোগ হবে না যদি না আপনি আবার চান।
- সেভ করার সময়, ফাইলের নাম পরিবর্তনের সময়, আর ফোল্ডার ডিলিট করার সময়
  কয়েকটা সূক্ষ্ম কিন্তু গুরুত্বপূর্ণ বাগ ঠিক করা হয়েছে যেগুলোতে ভুল
  ফাইলে ডেটা বসে যাওয়া বা ডুপ্লিকেট ফাইল তৈরি হওয়ার ঝুঁকি ছিল।
- অফলাইনে দ্রুত ফাইল-তালিকা ও নোট দেখানোর জন্য একটা লোকাল ক্যাশ সিস্টেম
  যোগ করা হয়েছে (অনেকটা Obsidian যেভাবে সাথে সাথে খোলে, সেরকম)।
- একটা নিরাপত্তা সমস্যা ঠিক করা হয়েছিল Worker-এ (কে GitHub-এ অ্যাক্সেস
  করতে পারবে তা সীমিত করা)।

*আরও পুরনো/বিস্তারিত ইতিহাস* `HISTORY.md`-*এ আছে, তবে সেটা টেকনিক্যাল
ভাষায় লেখা এবং সাধারণত পড়ার দরকার হয় না।*
